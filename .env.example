# ---------------------------------------------------------------------------
# 1. Meta (WhatsApp) Configuration
#    - Go to: https://developers.facebook.com/apps/
#    - Create App > Business > WhatsApp
#    - API Setup > "Temporary Access Token" (or System User Token for Production)
# ---------------------------------------------------------------------------
WHATSAPP_TOKEN=EAAG...

#    - From the same API Setup page: "Phone Number ID"
PHONE_NUMBER_ID=123456789012345

#    - Configuration > Webhook > Edit
#    - Create any random string here (e.g., "blue_mountains_2025")
#    - You must enter this SAME string in the Meta Dashboard to verify.
WHATSAPP_VERIFY_TOKEN=my_secret_verify_token

# ---------------------------------------------------------------------------
# 2. Google Gemini Configuration
#    - Go to: https://aistudio.google.com/app/apikey
# ---------------------------------------------------------------------------
GEMINI_API_KEY=AIza...

# ---------------------------------------------------------------------------
# 3. Redis Configuration
#    - If on Render/Railway with the provided config files, this is AUTO-SET.
#    - For local testing: redis://localhost:6379
# ---------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379

# ---------------------------------------------------------------------------
# 4. App Settings (Optional, defaults used if missing)
# ---------------------------------------------------------------------------
RATE_LIMIT_REQUESTS=20
RATE_LIMIT_WINDOW=60